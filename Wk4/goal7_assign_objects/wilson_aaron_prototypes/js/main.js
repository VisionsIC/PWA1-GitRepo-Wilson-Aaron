/** * Main JS Page Created by the JavaScript Development Team * Class: PWA 1 * Goal: Goal 7 *//** * Recreated by aaronrobertwilson on 2/28/15 * /** * Goal 7 - Prototypes * Date: 02/28/15 Assignment 4 - Prototypes */ /* -------------------JavaScript Begin------------------------------ */// Self Executing function.(function(){	// Console log to check the code for proper execution.    console.log("You're inside A function");	// Define 3 important variables: interval, names, and persons.    var interval =  setInterval(runUpdate, 100000 / 30);    var names = ["John","Jacob","Jingle","Himer","Schmidt"];    var persons = [];	// Console log to check the code for proper execution. (---inside function, and mainly checking 'names' variable for output---)    console.log(newPerson);	// Define 3 important variables: colOne, colTwo, and colThree. hese will allow for the fucntioning JS to inject the names in the array into the proper HTML tags.    var colOne = document.querySelector("#r3c1");    var colTwo = document.querySelector("#r3c2");    var colThree = document.querySelector("#r3c3");	// The start of a 'for' loop that generates the random activities of the persons from the array.    for(var i=0; i<3 ; i++) {		// Define 3 additional important variables: randomNamesIndex, randomNames, and row. These also will aid in the calculation of JS to inject the names in the array into the proper HTML tags.        var randomNamesIndex = Math.floor(Math.random()*names.length);        var randomNames = names[randomNamesIndex];        var row = i+1;		// Define 1 additional important variable: newPerson.         var newPerson = new Person(randomNames, row);        // Console log to check the code for proper execution. (---inside 'for' loop, and an array---)        console.log("You're in the array 'names' " + randomNamesIndex);        // Cycle through 'names' to output to HTML page.		persons.push(newPerson);		// Split the 'names' up to output to HTML page.        names.splice(randomNamesIndex,1);    }	// Create a function called 'generateHTML'.    function  generateHTML(){		// The start of a 'for' loop that generates the random name, job, and action of the persons from the array. If then distributes it to the proper row and column in the HTML page.        for(var i = 0; i < persons.length; i++){            document.querySelector("#r" + parseInt(i + 1) + "c1").innerHTML = persons[i].name;                        document.querySelector("#r" + parseInt(i + 1) + "c2").innerHTML = persons[i].job;            document.querySelector("#r" + parseInt(i + 1) + "c3").innerHTML = persons[i].action;        // Console log to check the code for proper execution. (---inside function - generateHTML, and mainly checking for proper placement and output---)		console.log("You should be seeing the 'name' in the array persons "+ persons[i].name);        }    }	// executes the JavaScript code to the HTML page.    generateHTML();	// Calls a final function to run the above executable code based upon the update of the time.    function runUpdate(){        persons.forEach(function(element){            element.update();        });        generateHTML();    }    interval();})();